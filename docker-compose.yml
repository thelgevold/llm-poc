services:
  app:
    build:
      context: .
    container_name: custom-llm-app
    working_dir: /app
    volumes:
      - .:/app
    command: watchfiles --filter python "python main.py"
    restart: unless-stopped
